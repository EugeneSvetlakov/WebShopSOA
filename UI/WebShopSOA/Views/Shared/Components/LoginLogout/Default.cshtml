@if (User.Identity.IsAuthenticated)
{
<div class="d-inline">
    <a asp-controller="Profile" asp-action="Index" class="btn btn-link"><i class="fa fa-lock"></i>Здравствуйте, @User.Identity.Name</a>
    <a asp-controller="Account" asp-action="Logout" class="btn btn-link"><i class="fa fa-unlock"></i>Выйти</a>
</div>
    @*<form method="post" asp-controller="Account" asp-action="Logout" asp-antiforgery="true" class="d-inline">
            <span>@User.Identity.Name | </span>
            <input type="submit" value="Выход" class="btn btn-link" />
    </form>*@
    
}
else
{
    <div class="d-inline p-2">
        <a asp-action="Login" asp-controller="Account" class="btn btn-link"><i class="fa fa-unlock"></i>Войти</a>
        <a asp-action="Register" asp-controller="Account" class="btn btn-link"><i class="fa fa-user"></i>Регистрация</a>
    </div>
}
